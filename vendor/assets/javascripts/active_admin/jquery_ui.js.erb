<% jquery_ui_path = if Jquery::Ui::Rails::VERSION >= "5"
                      "jquery-ui/"
                    else
                      "jquery.ui."
                    end
%>
<% require_asset "#{jquery_ui_path}widgets/datepicker" %>
<% require_asset "#{jquery_ui_path}widgets/dialog" %>
<% require_asset "#{jquery_ui_path}widgets/sortable" %>
<% require_asset "#{jquery_ui_path}widgets/tabs" %>
<% require_asset "#{jquery_ui_path}widget" %>
